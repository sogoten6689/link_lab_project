<%= form_for(@raw_test, :url => (@raw_test.new_record? ? admin_data_management_raw_tests_path : admin_data_management_raw_test_path(@raw_test))) do |f| %>
  <div class="card-body">
    <div class="row">
      <div class="col-sm-6">
        <div class="form-group">
          <label for="name">
            <%= t('raw_tests.fields.name') %>
          </label>
          <%= f.text_field :name, class: "form-control", id: "name", placeholder: t('raw_tests.fields.name')%>
          <% @raw_test.errors.full_messages_for(:name).each do |message| %>
            <div class="text-danger"><%= message %></div>
          <% end %>
        </div>
        <div class="form-group">
          <label for="cost">
            <%= t('raw_tests.fields.cost') %>
          </label>
          <%= f.number_field :cost, class: "form-control", id: "cost", placeholder: t('raw_tests.fields.cost')%>
          <% @raw_test.errors.full_messages_for(:cost).each do |message| %>
            <div class="text-danger"><%= message %></div>
          <% end %>
        </div>
        <div class="form-group">
          <label for="tax">
            <%= t('raw_tests.fields.tax') %>
          </label>
          <%= f.number_field :tax, class: "form-control", id: "tax", placeholder: t('raw_tests.fields.tax')%>
          <% @raw_test.errors.full_messages_for(:tax).each do |message| %>
            <div class="text-danger"><%= message %></div>
          <% end %>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="form-group">
          <label for="name">
            <%= t('raw_tests.fields.test_type') %>
          </label>
          <%= f.select(:test_type_id, @test_types.collect {|u| [u.vi_name, u.id]},{},{ :class => 'form-control select2' }) %>
          <% @raw_test.errors.full_messages_for(:test_type_id).each do |message| %>
            <div class="text-danger"><%= message %></div>
          <% end %>
        </div>
        <div class="form-group">
          <label for="note">
            <%= t('raw_tests.fields.note') %>
          </label>
          <%= f.text_field :note, class: "form-control", id: "note", placeholder: t('raw_tests.fields.note') %>
          <% @raw_test.errors.full_messages_for(:note).each do |message| %>
            <div class="text-danger"><%= message %></div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="card-footer">
    <button type="submit" class="btn btn-primary">
      <%= @raw_test.new_record? ? t('button.new_btn') : t('button.edit_btn') %>
    </button>
    <%= link_to t('button.cancel_btn'), admin_data_management_raw_tests_path, class: "btn btn-danger"%>
  </div>
<% end %>
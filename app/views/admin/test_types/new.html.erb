<div class="content container-fluid">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <div class="container-fluid">
    <div class="row">
      <!-- left column -->
      <div class="col-md-12">
        <!-- general form elements -->
        <div class="card card-primary">
          <div class="card-header">
            <h3 class="card-title">
              <%= t('testTypes.new_test_type') %>
            </h3>
          </div>
          <h1>
            <%= @test_type.id %>
          </h1>
          <%= render "form" %>
        </div>
      </div>
    </div>
  </div>
</div>
